Database provider configuration is tested here before its matching agent image is released.
While this marker exists, chart version, appVersion, image tag, and image digest stay unchanged.
To publish, remove this marker, bump version and appVersion, and pin the compatible new image digest.
